import { Text, View } from "react-native";

export default function Overview() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        backgroundColor: "red"
      }}
    >
      <Text>Overview.</Text>
    </View>
  );
}
