export enum Goal {
    GainWeightFast = 0, // Gain weight quickly
    GainWeight = 1, // Gain weight at a moderate pace
    MaintainWeight = 2, // Maintain current weight
    LoseWeight = 3, // Lose weight at a moderate pace
    LoseWeightFast = 4, // Lose weight quickly
}